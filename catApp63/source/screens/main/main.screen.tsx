import React, {useState, useEffect, useLayoutEffect, useContext} from 'react';
import {
  SafeAreaView,
  Image,
  Easing,
  View,
  Text,
  Animated,
  ScrollView,
  Button,
} from 'react-native';

const MainScreen = () => {
  return (
    <View style={{flex: 1}}>
      <Text style={{color: '#c9d1d9'}}>MainScreen</Text>
    </View>
  );
};

export default MainScreen;
